# 任务记录：纯 Codex 主模板与 rules 生成器

## Metadata

- 任务 ID：2026-05-08-2000-纯-Codex-主模板与-rules-生成器
- 负责 Agent：codex
- 最近接手 Agent：codex
- 状态：已完成
- 创建时间：2026-05-08 20:00
- 最近更新：2026-05-08 20:00
- 关联状态：`docs/agents/PROJECT_STATE.md`
- 关联工作日志：`docs/agents/WORKLOG.md`

## Request

```text
把 Cursor_Kit 改成纯 Codex 主模板：根目录只保留 AGENTS.md，Claude Code / Trae rules 按同一逻辑通过工具生成到目标项目。
```

## Goal

交付一个纯 Codex 根模板，并提供 `export-rules` 命令按需生成 Claude Code / Trae rules。

## Acceptance Criteria

- [x] 根目录不再把 `CLAUDE.md` 和 `.trae/project_rules.md` 作为主动入口。
- [x] `tools/agent_context.py export-rules` 可生成 Claude Code / Trae rules，默认拒绝覆盖，`--force` 可覆盖。
- [x] `new-task --agent` 默认值为 `codex`，仍允许 `claude` / `trae`。
- [x] README、AGENTS 和 `docs/agents/` 文档统一为纯 Codex 主模板口径。
- [x] `python3 -m unittest tests/test_agent_context.py`、`python3 tools/agent_context.py validate`、`python3 tools/agent_context.py status` 通过。

## Decisions

- “纯 Codex”指根模板主动入口只服务 Codex，不删除派生 Claude/Trae rules 的能力。
- `docs/agents/` 暂不改名，继续作为可被派生 rules 复用的任务上下文目录。
- Claude/Trae rules 生成到目标项目，不在 Cursor_Kit 根仓库保留生成产物。

## Session Notes

### 2026-05-08 20:00 - codex

- 做了什么：创建任务记录；按 TDD 更新测试；实现 `export-rules`、`new-task` 默认 `codex`、纯 Codex `validate`；删除根目录 Claude/Trae 主动入口；同步 README、AGENTS、`docs/agents/` 和版本号。
- 关键文件：`AGENTS.md`、`README.md`、`docs/agents/`、`tools/agent_context.py`、`tests/test_agent_context.py`、`.cursor-kit-version`。
- 验证：`python3 -m unittest tests/test_agent_context.py`、`python3 tools/agent_context.py validate`、`python3 tools/agent_context.py status`。
- 下一步：继续迭代前先运行 `python3 tools/agent_context.py status`。

## Handoff

- 给下一个 Agent 先看：本任务记录、`tools/agent_context.py`、`tests/test_agent_context.py`。
- 当前风险：无已知阻塞；后续修改要避免重新把生成的 `CLAUDE.md` 或 `.trae/project_rules.md` 提交进根模板。
- 建议下一步：需要兼容 Claude Code / Trae 时使用 `python3 tools/agent_context.py export-rules --agent all --target-root /path/to/project`。
